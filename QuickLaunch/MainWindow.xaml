<Window x:Class="QuickLaunch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="QuickLaunch"
        Width="760"
        Height="590"
        MinWidth="560"
        MinHeight="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Deactivated="Window_Deactivated"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#F91B1D29"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#24334355"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#FF9BA7B8"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFF5F7FA"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF8B7CFF"/>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontSize" Value="21"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style TargetType="{x:Type ListBox}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="ItemBorder" Padding="12,10" CornerRadius="10" Background="Transparent">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FF2D3042"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FF252936"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="14"
            Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource PanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="18">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="8" Opacity="0.45" Color="#FF000000"/>
        </Border.Effect>
        <Grid Margin="30,26,30,22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,22">
                <StackPanel Orientation="Horizontal">
                    <Border Width="34" Height="34" CornerRadius="10" Background="{StaticResource AccentBrush}" Margin="0,0,12,0">
                        <TextBlock Text="Q" Foreground="White" FontSize="19" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="QuickLaunch" Foreground="{StaticResource TextBrush}" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="Find anything, get anywhere" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
                <Button Content="×" Click="CloseButton_Click" Width="30" Height="30" HorizontalAlignment="Right"
                        Background="Transparent" BorderThickness="0" Foreground="{StaticResource MutedBrush}"
                        FontSize="22" Cursor="Hand" ToolTip="Close"/>
            </Grid>

            <Border Grid.Row="1" Background="#FF252936" BorderBrush="#FF3A3E50" BorderThickness="1"
                    CornerRadius="12" Padding="17,13" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="⌕" FontSize="26" Foreground="{StaticResource AccentBrush}" Margin="0,-2,13,0"/>
                    <TextBox x:Name="SearchBox" Grid.Column="1" TextChanged="SearchBox_TextChanged"
                             VerticalContentAlignment="Center" ToolTip="Search apps, folders, and commands"/>
                    <Border Grid.Column="2" Background="#FF3A3E50" CornerRadius="6" Padding="7,3" Margin="12,0,0,0">
                        <TextBlock Text="Ctrl + Space" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <ListBox x:Name="ResultsList" ItemsSource="{Binding Results}" SelectionChanged="ResultsList_SelectionChanged"
                         MouseDoubleClick="ResultsList_MouseDoubleClick" KeyDown="ResultsList_KeyDown">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="42"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" CornerRadius="9" Background="{Binding AccentColor}">
                                    <TextBlock Text="{Binding Glyph}" Foreground="White" FontSize="15" FontWeight="SemiBold"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Description}" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,3,0,0"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="↵" Foreground="#FF647084" FontSize="16" VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <StackPanel x:Name="EmptyState" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="No matches" Foreground="{StaticResource TextBrush}" FontSize="17" FontWeight="SemiBold"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Try a different search term" Foreground="{StaticResource MutedBrush}" FontSize="12"
                               Margin="0,8,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="3" Margin="0,16,0,0">
                <TextBlock x:Name="StatusText" Text="Ready" Foreground="{StaticResource MutedBrush}" FontSize="11"
                           VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="↑↓" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                    <TextBlock Text=" navigate" Foreground="#FF647084" FontSize="11" Margin="4,0,14,0"/>
                    <TextBlock Text="Enter" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                    <TextBlock Text=" open" Foreground="#FF647084" FontSize="11" Margin="4,0,14,0"/>
                    <TextBlock Text="Esc" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                    <TextBlock Text=" hide" Foreground="#FF647084" FontSize="11" Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
